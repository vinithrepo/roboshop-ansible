#component=catalogue

#rpm_type=nodejs
#schema_type=mongodb

#source common.sh
#func_nodejs

- name: Catalogue setup
  hosts: all
  become: true
  vars:
    component: catalogue
  tasks:
    - name: importing nodejs file
      ansible.builtin.import_tasks: nodejs.yml

